{{ config(materialized = 'view') }}

select
  id,
  client_id,
  publisher_id,
  organization_id,
  activity,
  domain_original,
  type,
  status_code,
  status,
  priority,
  tags,
  tasks,
  audience,
  soft_skills,
  requirements,
  rome_skills,
  reduced_mobility_accessible,
  close_to_transport,
  open_to_minors,
  remote,
  schedule,
  duration,
  posted_at,
  start_at,
  end_at,
  places,
  places_status,
  deleted_at,
  created_at,
  updated_at,
  domain,
  is_deleted
from {{ ref('int_mission') }}
